---
layout: layout.njk
---

<div class="wrapper">
	<article class="stack">
		<header class="page-header">
			<h1>{{ title }}</h1> 
			<time datetime="{{ date | dateIso }}">{{ date | dateReadable }}</time> 
		</header>
		{{ content | safe }}
	</article>
</div>